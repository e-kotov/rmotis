

Always assume we are testing with local MOTIS instance at http://localhost:8080

See the API documentation of MOTIS in `private/openapi.yaml`

Always put any test files or artifacts into `local-testing` folder so that they are not tracked. This folder is in `.gitignore`, but do use it and allow yourself access to it.
