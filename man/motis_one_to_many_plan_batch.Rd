% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_one_to_many_plan_batch.R
\name{motis_one_to_many_plan_batch}
\alias{motis_one_to_many_plan_batch}
\title{Multi-origin Batch Planning for One-to-Many Routing}
\usage{
motis_one_to_many_plan_batch(
  one,
  many,
  output_file,
  mode = c("WALK", "BIKE", "CAR"),
  arrive_by = FALSE,
  max = 7200,
  maxMatchingDistance = 1000,
  one_id_col = "id",
  many_id_col = "id",
  withDistance = FALSE,
  ...,
  spatial_filter_km = NULL,
  spatial_sort = TRUE,
  progress = TRUE,
  api_endpoint = "/api/v1/one-to-many",
  duration_key = "max"
)
}
\arguments{
\item{one}{Origins. Same as \code{\link[=motis_one_to_many_batch]{motis_one_to_many_batch()}}.}

\item{many}{Destinations. Same as \code{\link[=motis_one_to_many_batch]{motis_one_to_many_batch()}}.}

\item{output_file}{Path to the output query file.}

\item{mode}{Travel mode: \code{"WALK"}, \code{"BIKE"}, or \code{"CAR"}.}

\item{arrive_by}{Logical. If \code{TRUE}, treat \code{one} as destinations and
\code{many} as origins (many-to-one).}

\item{max}{Maximum travel time in seconds. Defaults to \code{7200} (2 hours).}

\item{maxMatchingDistance}{Maximum map-matching distance in meters.
Defaults to \code{1000}.}

\item{one_id_col}{Column name for origin IDs. Defaults to \code{"id"}.}

\item{many_id_col}{Column name for destination IDs. Defaults to \code{"id"}.}

\item{withDistance}{Logical. Include distance in the output?
Defaults to \code{FALSE}.}

\item{...}{Additional API parameters passed to the MOTIS one-to-many endpoint.}

\item{spatial_filter_km}{Numeric. Optional straight-line distance threshold
(in kilometers). If provided, destinations further than this distance from
an origin will be excluded from the batch query for that origin. Reduces
query file size and server processing load.}

\item{spatial_sort}{Logical. If \code{TRUE} (default), sort origins by latitude
before generating queries. Improves MOTIS graph cache locality when
processing the batch file.}

\item{progress}{Logical. If \code{TRUE} (default), display progress messages.}

\item{api_endpoint}{API endpoint path. Defaults to \code{"/api/v1/one-to-many"}.}

\item{duration_key}{The name of the duration parameter in the query.
Defaults to \code{"max"}.}
}
\value{
Invisibly returns a list with:
\itemize{
\item \code{file}: Path to the output file
\item \code{n_lines}: Number of query lines generated
\item \code{file_size}: File size in bytes
}
}
\description{
Generate a batch query file for multiple origins with spatial optimizations.
This function wraps \code{\link[=motis_one_to_many_generate_batch]{motis_one_to_many_generate_batch()}} and iterates over
origins, applying spatial sorting and filtering for improved performance.
}
\section{Relationship to Other Functions}{

This function is a convenience wrapper that:
\enumerate{
\item Sorts origins by latitude (\code{spatial_sort = TRUE})
\item For each origin:
\itemize{
\item Filters destinations to a bounding box (\code{spatial_filter_km})
\item Calls \code{\link[=motis_one_to_many_generate_batch]{motis_one_to_many_generate_batch()}} with \code{append = TRUE}
}
\item Returns file metadata
}
}

\section{Spatial Optimizations}{

\itemize{
\item \strong{Spatial sort}: Origins are sorted by latitude before iteration. This
improves MOTIS graph cache hits when processing the batch file sequentially.
\item \strong{Spatial filter}: For each origin, destinations are filtered to a
bounding box based on \code{spatial_filter_km} and a 5\% buffer.
This reduces unnecessary map-matching attempts and response I/O.
}
}

\seealso{
\code{\link[=motis_one_to_many_batch]{motis_one_to_many_batch()}}, \code{\link[=motis_one_to_many_generate_batch]{motis_one_to_many_generate_batch()}}
}
