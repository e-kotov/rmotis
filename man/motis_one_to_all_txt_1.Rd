% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motis_one_to_all_txt_1.R
\name{motis_one_to_all_txt_1}
\alias{motis_one_to_all_txt_1}
\title{Builds a MOTIS one-to-all request manually and saves it to a text file.}
\usage{
motis_one_to_all_txt_1(
  one,
  output_file,
  time = Sys.time(),
  arrive_by = FALSE,
  max_travel_time = 90,
  ...
)
}
\arguments{
\item{one}{The single origin (when \code{arrive_by = FALSE}) or destination
(when \code{arrive_by = TRUE}).}

\item{output_file}{The path to the output text file where the request URL
will be written.}

\item{time}{The departure or arrival time. Defaults to \code{Sys.time()}.}

\item{arrive_by}{Logical. If \code{FALSE} (the default), calculates routes from
\code{one}. If \code{TRUE}, calculates routes to \code{one}.}

\item{max_travel_time}{Integer. The maximum travel time in minutes. Defaults to 120.}

\item{...}{Additional parameters to be passed to the MOTIS One-to-All API,
(e.g., \code{maxTransfers}, \code{transitModes}).}
}
\value{
Invisibly returns a character vector containing the generated request
string. The primary side effect is writing this string to \code{output_file}.
}
\description{
This function efficiently constructs the API request for a one-to-all
query by building the URL string manually. It writes the resulting URL
(path and query string) to a specified text file. This is useful for
scenarios where requests are generated in one step and executed later,
for example with a bulk processing tool.
}
