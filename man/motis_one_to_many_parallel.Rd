% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_one_to_many_parallel.R
\name{motis_one_to_many_parallel}
\alias{motis_one_to_many_parallel}
\title{Parallel One-to-Many Routing via HTTP}
\usage{
motis_one_to_many_parallel(
  one,
  many,
  mode = c("WALK", "BIKE", "CAR"),
  arrive_by = FALSE,
  max = 7200,
  maxMatchingDistance = 1000,
  one_id_col = "id",
  many_id_col = "id",
  ...,
  .server = getOption("rmotis.server", "http://localhost:8080"),
  batch_size = 16L,
  output_file = NULL,
  checkpoint_file = NULL,
  spatial_filter = TRUE,
  spatial_sort = TRUE,
  max_speed_kmh = NULL,
  progress = TRUE
)
}
\arguments{
\item{one}{Origins. Can be sf, data.frame with lat/lon columns, matrix, or
character vector of \code{"lat;lon"} strings.}

\item{many}{Destinations. Same format as \code{one}.}

\item{mode}{Travel mode: \code{"WALK"}, \code{"BIKE"}, or \code{"CAR"}.}

\item{arrive_by}{Logical. If \code{TRUE}, treat \code{one} as destinations and \code{many}
as origins (many-to-one).}

\item{max}{Maximum travel time in seconds. Defaults to \code{7200} (2 hours).}

\item{maxMatchingDistance}{Maximum map-matching distance in meters.
Defaults to \code{1000}.}

\item{one_id_col}{Column name for origin IDs. Defaults to \code{"id"}.}

\item{many_id_col}{Column name for destination IDs. Defaults to \code{"id"}.}

\item{...}{Additional API parameters passed to the MOTIS one-to-many endpoint.}

\item{.server}{MOTIS server URL. Defaults to \code{getOption("rmotis.server")} or
\code{"http://localhost:8080"}.}

\item{batch_size}{Number of origins to process in parallel per batch.
Defaults to \code{16}. Set based on server capacity and network conditions.}

\item{output_file}{Optional output file path:
\itemize{
\item \code{.csv}: Appended incrementally with \code{data.table::fwrite()} (or
\code{utils::write.table()} fallback).
\item \code{.parquet}: Creates a \emph{directory} at this path containing
\code{batch_0001.parquet}, \code{batch_0002.parquet}, etc. Read with
\code{arrow::open_dataset(path) |> collect()}.
\item \code{NULL} (default): Accumulate results in memory and return as data.frame.
}}

\item{checkpoint_file}{Optional checkpoint file path. If provided, the
function will:
\itemize{
\item On first run: create the checkpoint file and log completed origin IDs.
\item On subsequent runs: skip already-completed origins and resume from where
it left off.
Checkpoint format is plain text (one origin ID per line) for human
readability.
}}

\item{spatial_filter}{Logical. If \code{TRUE} (default), pre-filter destinations
per origin to a bounding box based on \code{max} travel time and mode speed.}

\item{spatial_sort}{Logical. If \code{TRUE} (default), sort origins by latitude
before processing. Improves cache locality if the MOTIS server maintains
graph caches.}

\item{max_speed_kmh}{Optional custom max speed in km/h for spatial filtering.
If \code{NULL}, uses mode defaults (WALK=6, BIKE=20, CAR=130).}

\item{progress}{Logical. If \code{TRUE} (default), display progress messages.}
}
\value{
\itemize{
\item If \code{output_file = NULL}: Returns a data.frame with columns \code{from_id},
\code{to_id}, \code{duration_s}, and optionally \code{distance_m}.
\item If \code{output_file} is specified: Returns the file path invisibly.
}
}
\description{
Execute one-to-many routing requests in parallel using httr2. This function
bypasses the MOTIS batch CLI and sends HTTP POST requests directly to a
running MOTIS server, enabling checkpointing and incremental output.
}
\section{Checkpointing}{

Checkpoints are crucial for long-running jobs. If the process is interrupted,
simply re-run the same command with the same \code{checkpoint_file} path to resume.
}

\section{Output Formats}{

\itemize{
\item \strong{CSV}: Efficient for streaming appends. Header written once.
\item \strong{Parquet directory}: Ideal for large datasets. Each batch is a separate
file. Use \code{arrow::open_dataset()} to read all batches at once.
\item \strong{In-memory}: Convenient for small datasets or interactive use.
}
}

\seealso{
\code{\link[=motis_one_to_many_batch]{motis_one_to_many_batch()}}, \code{\link[=motis_one_to_many]{motis_one_to_many()}}
}
