% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_one_to_many.R
\name{motis_one_to_many_generate_batch}
\alias{motis_one_to_many_generate_batch}
\title{Generate MOTIS Batch Query File for One-to-Many}
\usage{
motis_one_to_many_generate_batch(
  one,
  many,
  output_file,
  mode = c("WALK", "BIKE", "CAR"),
  arrive_by = FALSE,
  max = 7200,
  maxMatchingDistance = 1000,
  ...,
  append = FALSE,
  api_endpoint = "/api/v1/one-to-many"
)
}
\arguments{
\item{one}{The single origin (when \code{arrive_by = FALSE}) or destination
(when \code{arrive_by = TRUE}).}

\item{many}{The multiple destinations (when \code{arrive_by = FALSE}) or origins
(when \code{arrive_by = TRUE}).}

\item{output_file}{The path to the output text file.}

\item{mode}{The routing profile to use (WALK, BIKE, CAR).}

\item{arrive_by}{Logical. If \code{FALSE} (the default), calculates routes from
\code{one} to \code{many}. If \code{TRUE}, calculates routes from \code{many} to \code{one}.}

\item{max}{maximum travel time in seconds}

\item{maxMatchingDistance}{maximum matching distance in meters}

\item{...}{Additional MOTIS API parameters.}

\item{append}{Logical. If \code{TRUE}, appends to \code{output_file}.}

\item{api_endpoint}{The API path. Defaults to \code{"/api/v1/one-to-many"}.}
}
\value{
Invisibly returns the number of queries written (always 1 for this endpoint, but vectorized over many).
}
\description{
Efficiently constructs a text file of MOTIS one-to-many street routing
queries for batch processing.
}
