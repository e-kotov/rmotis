% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{motis_install}
\alias{motis_install}
\title{Install MOTIS Backend Binaries}
\usage{
motis_install(
  version = "latest",
  location = "cache",
  path = NULL,
  force = FALSE,
  path_action = c("both", "session", "project", "none"),
  quiet = FALSE
)
}
\arguments{
\item{version}{A string specifying the MOTIS version tag to install.
Defaults to \code{"latest"}, which automatically finds the most recent stable
version by calling \code{motis_check_latest_version()}.}

\item{location}{A string specifying the type of installation location.
One of:
\itemize{
\item \code{"cache"} (default): Installs to a persistent, user-level cache
directory recommended by \code{tools::R_user_dir("rmotis", "cache")}.
\item \code{"project"}: Installs to a \code{./bin/motis/{platform-arch}} subdirectory
within the current project.
}}

\item{path}{An optional string specifying a custom directory path for the
installation. If provided, \code{location} must not be set.}

\item{force}{A logical value. If \code{TRUE}, reinstall MOTIS even if it's already
found in the target directory. Defaults to \code{FALSE}.}

\item{path_action}{A string specifying how to handle the system \code{PATH}. One of:
\itemize{
\item \code{"session"}: Adds the MOTIS directory to \code{PATH} for the current R
session only.
\item \code{"project"}: Modifies the \code{.Rprofile} in the current project to
set the \code{PATH} for future sessions in that project.
\item \code{"both"} (default): Performs both the \code{"session"} and \code{"project"} actions.
\item \code{"none"}: Does not modify the \code{PATH}.
}}

\item{quiet}{A logical value. If \code{TRUE}, suppresses messages and confirmation
prompts. Defaults to \code{FALSE}.}
}
\value{
The path to the installation directory, invisibly.
}
\description{
Downloads and installs pre-compiled binaries for the MOTIS backend from the
official GitHub releases.
}
\details{
This function automates the download and setup of MOTIS. It performs the
following steps:
\enumerate{
\item Queries the GitHub API to find the specified MOTIS release.
\item Identifies the correct binary archive for the user's OS and architecture.
\item Downloads and extracts the archive.
\item Copies the MOTIS executable and its supporting files to the specified
installation directory.
\item Optionally modifies the \code{PATH} environment variable for the current
session and/or the current project's \code{.Rprofile}.
}

Please provide either the \code{location} OR the \code{path} argument, but not both.
}
\examples{
\dontrun{
# Install to project and set PATH for both session and .Rprofile
motis_install(location = "project", path_action = "both")

# Install to cache and set PATH for session only
motis_install(location = "cache", path_action = "session")

# Clean up the project's .Rprofile and session PATH
motis_clear_path()
}
}
