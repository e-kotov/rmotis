% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processx.R
\name{motis_configure_import}
\alias{motis_configure_import}
\title{Configure MOTIS Import Settings}
\usage{
motis_configure_import(path, ..., force = FALSE)
}
\arguments{
\item{path}{A string. Path to the root MOTIS directory or the \code{config.yml} file.}

\item{...}{Named arguments for configuration.}

\item{force}{A logical. If \code{FALSE} (default), asks for confirmation.}
}
\value{
The path to the modified \code{config.yml}, invisibly.
}
\description{
Updates the \code{config.yml} file in the root directory, which is used during
the \code{motis_import()} process. This allows you to adjust how the data is
preprocessed (e.g., matching distances, footpath lengths).
}
\section{Import Configuration Options}{


\strong{Timetable Settings (\code{timetable}):}
\itemize{
\item \code{first_day}: First day of timetable to load ("YYYY-MM-DD" or "TODAY").
\item \code{num_days}: Number of days to load (default: 365).
\item \code{railviz}: Enable real-time vehicle visualization (default: true).
\item \code{with_shapes}: Extract and serve shapes (default: true).
\item \code{adjust_footpaths}: Adjust footpaths if they are too fast (default: true).
\item \code{link_stop_distance}: Max distance in meters to link stops (default: 100).
\item \code{max_footpath_length}: Max footpath length in minutes (default: 15).
\item \code{max_matching_distance}: Max distance from geolocation to OSM ways (default: 25.0).
}
}

