% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_path.R
\name{motis_clear_path}
\alias{motis_clear_path}
\title{Clear MOTIS Path from .Rprofile and Restore Session PATH}
\usage{
motis_clear_path(project_path = getwd(), quiet = FALSE)
}
\arguments{
\item{project_path}{The path to the project directory containing the
\code{.Rprofile} file. Defaults to the current working directory (\code{getwd()}).}

\item{quiet}{A logical value. If \code{TRUE}, suppresses messages. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns \code{TRUE} if the \code{.Rprofile} was modified, \code{FALSE} otherwise.
}
\description{
Removes MOTIS-specific configuration from the project's \code{.Rprofile} and
restores the \code{PATH} environment variable for the current R session to its
state before \code{motis_install()} was called.
}
\details{
This function performs two actions:
\enumerate{
\item It restores the \code{PATH} for the current R session using a backup stored in
\code{options("rmotis.original_path")}. If the backup is not found, a message
is printed.
\item It reads the \code{.Rprofile} file in the current working directory, removes
any lines containing the special \verb{#-added-by-r-pkg-rmotis} comment, and
writes the cleaned content back to the file.
}
}
\examples{
\dontrun{
# Assuming motis_install(path_action = "project") was run previously

# Clean up the project's .Rprofile and restore the session PATH
motis_clear_path()
}
}
