% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_versions.R
\name{motis_check_available_versions}
\alias{motis_check_available_versions}
\title{Check for Available MOTIS Versions}
\usage{
motis_check_available_versions(prereleases = FALSE)
}
\arguments{
\item{prereleases}{A logical value. If \code{TRUE}, include pre-release versions
in the returned table. Defaults to \code{FALSE}.}
}
\value{
A \code{tibble} (or \code{data.frame}) with the following columns:
\item{tag_name}{The version tag string.}
\item{published_at}{The publication timestamp of the release.}
\item{prerelease}{A logical indicating if it is a pre-release.}
\item{linux_amd64}{Logical, \code{TRUE} if a binary is available for this platform.}
\item{macos_arm64}{Logical, \code{TRUE} if a binary is available for this platform.}
\item{windows_amd64}{Logical, \code{TRUE} if a binary is available for this platform.}
}
\description{
Queries the GitHub API to find all MOTIS releases and reports which ones
have pre-compiled binaries for major platforms.
}
\details{
This function scans all releases in the \code{motis-project/motis} GitHub
repository. For each release, it checks for asset files matching common
platform binaries (e.g., \code{motis-linux-amd64.tar.bz2}). The result is a
table showing the availability of these binaries for each version tag.
}
\examples{
\dontrun{
# Get a table of all stable versions with available binaries
available_versions <- motis_check_available_versions()
print(available_versions)
}
}
