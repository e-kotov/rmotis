% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_one_to_many.R
\name{motis_one_to_many_read_batch}
\alias{motis_one_to_many_read_batch}
\title{Read MOTIS Batch Response File for One-to-Many}
\usage{
motis_one_to_many_read_batch(
  response_file,
  metadata_file,
  arrive_by = FALSE,
  chunk_size = 10000L,
  output_callback = NULL
)
}
\arguments{
\item{response_file}{Path to the MOTIS batch response file (one JSON array
per line).}

\item{metadata_file}{Path to the metadata sidecar file generated by
\code{\link[=motis_one_to_many_generate_batch]{motis_one_to_many_generate_batch()}} (the \code{.meta} file).}

\item{arrive_by}{Logical. If \code{FALSE} (default), the \code{one} in each query was
the origin. If \code{TRUE}, the \code{one} was the destination.}

\item{chunk_size}{Number of lines to read and process at a time. Larger
values use more memory but may be faster. Defaults to \code{10000L}.}

\item{output_callback}{Optional function that receives each processed chunk
(a data.frame) as its argument. When provided, chunks are streamed to the
callback instead of accumulated in memory, and the function returns
\code{invisible(NULL)}. Useful for writing results directly to Arrow, DuckDB,
or CSV.}
}
\value{
A data.frame with columns \code{from_id}, \code{to_id}, \code{duration_s}, and
optionally \code{distance_m}. Returns \code{invisible(NULL)} when \code{output_callback}
is used.
}
\description{
Parses a MOTIS batch response file and maps results back to origin and
destination IDs using a metadata sidecar file. Processes the files in chunks
for memory efficiency with large files.
}
