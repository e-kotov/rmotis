% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processx.R
\name{motis_configure_server}
\alias{motis_configure_server}
\title{Configure a MOTIS Server}
\usage{
motis_configure_server(path, ..., force = FALSE)
}
\arguments{
\item{path}{A string. Path to the \code{config.yml} file, the \verb{data/} directory,
or the root MOTIS project directory.}

\item{...}{Named arguments representing the configuration structure. Nested
keys can be provided as named lists.}

\item{force}{A logical. If \code{FALSE} (default), asks for confirmation in
interactive sessions before modifying the file.}
}
\value{
The path to the modified \code{config.yml}, invisibly.
}
\description{
Updates the \code{config.yml} file used for running the MOTIS server (typically
located in the \verb{data/} subdirectory). This function can automatically
identify the data directory even if you provide the path to the root
project folder.
}
\section{Configuration Options}{


\strong{Server Settings (\code{server}):}
\itemize{
\item \code{host}: The IP address for the server to bind to (default: "0.0.0.0").
\item \code{port}: The TCP port for the server (default: 8080).
\item \code{web_folder}: Folder with static files to serve (e.g., "ui").
\item \code{n_threads}: Number of hardware threads to use (default: auto).
\item \code{data_attribution_link}: Link to data sources or license.
}

\strong{Routing Limits (\code{limits}):}
\itemize{
\item \code{stoptimes_max_results}: Max results for stoptimes (default: 256).
\item \code{plan_max_results}: Max results for plan queries (default: 256).
\item \code{plan_max_search_window_minutes}: Max search window in minutes (max: 21600).
\item \code{onetoall_max_results}: Max results for one-to-all queries.
\item \code{onetoall_max_travel_minutes}: Max travel duration for one-to-all.
\item \code{routing_max_timeout_seconds}: Max duration for a routing query (default: 90).
\item \code{street_routing_max_prepost_transit_seconds}: Limit for maxPre/PostTransitTime.
\item \code{street_routing_max_direct_seconds}: Limit for maxDirectTime.
}

\strong{Modules:}
\itemize{
\item \code{street_routing}: Enable street routing (Boolean or list with \code{elevation_data_dir}).
\item \code{geocoding}: Enable geocoding (Boolean).
\item \code{reverse_geocoding}: Enable reverse geocoding (Boolean).
\item \code{osr_footpath}: Enable OSR footpath routing (Boolean).
}
}

\examples{
\dontrun{
# Update server port in the data directory
motis_configure_server(
  "~/motis-project",
  server = list(port = 8081)
)

# Increase routing limits
motis_configure_server(
  "~/motis-project/data",
  limits = list(plan_max_results = 500)
)
}
}
