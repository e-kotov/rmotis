% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_one_to_many_intermodal.R
\name{motis_one_to_many_intermodal}
\alias{motis_one_to_many_intermodal}
\title{Calculate one-to-many or many-to-one intermodal (public transit) routes}
\usage{
motis_one_to_many_intermodal(
  one,
  many,
  time = Sys.time(),
  arrive_by = FALSE,
  max_travel_time = 60,
  one_id_col = "id",
  many_id_col = "id",
  maxMatchingDistance = 1000,
  withDistance = FALSE,
  ...,
  engine = c("api", "batch"),
  output = c("data.frame", "raw_list"),
  parallel = TRUE,
  backend = c("auto", "httr2", "mirai"),
  batch_size = NULL,
  max_destinations_per_batch = NULL,
  output_path = NULL,
  checkpoint_file = NULL,
  progress = TRUE,
  data_dir = NULL,
  temp_dir = tempdir(),
  keep_files = FALSE,
  eol = NULL,
  motis_path = NULL
)
}
\arguments{
\item{one}{Origin(s). Can be a data frame/tibble with coordinate columns,
an \code{sf} object, or a vector/matrix of coordinates.}

\item{many}{Destination(s). Same format as \code{one}.}

\item{time}{The departure or arrival time. Can be a POSIXct object (like from
\code{Sys.time()}) or a character string in ISO 8601 format (e.g., "2025-08-15T15:11:00Z").
Defaults to the current time.}

\item{arrive_by}{Logical. If \code{FALSE} (the default), calculates routes from
\code{one} to \code{many}. If \code{TRUE}, calculates routes from \code{many} to \code{one}.}

\item{max_travel_time}{The maximum travel time in \strong{minutes}.}

\item{one_id_col}{The name of the column in \code{one} to use for identifying
the point in the output. Defaults to \code{"id"}.}

\item{many_id_col}{The name of the column in \code{many} to use for identifying
the point in the output. Defaults to \code{"id"}.}

\item{maxMatchingDistance}{maximum matching distance in meters to match geo
coordinates to the street network}

\item{withDistance}{Logical. Include distance in the output?
Defaults to \code{FALSE}. \strong{Note:} Currently ignored by the intermodal API.}

\item{...}{
  Arguments passed on to \code{\link[motis.client:mc_oneToManyIntermodalPost]{motis.client::mc_oneToManyIntermodalPost}}
  \describe{
    \item{\code{maxTransfers}}{\if{html}{\out{<integer>}} The maximum number of allowed transfers (i.e. interchanges between transit legs, pre- and postTransit do not count as transfers). \code{maxTransfers=0} searches for direct transit connections without any transfers. If you want to search only for non-transit connections (\code{FOOT}, \code{CAR}, etc.), send an empty \code{transitModes} parameter instead. If not provided, the routing uses the server-side default value which is hardcoded and very high to cover all use cases. \emph{Warning}: Use with care. Setting this too low can lead to optimal (e.g. the fastest) journeys not being found. If this value is too low to reach the destination at all, it can lead to slow routing performance.}
    \item{\code{minTransferTime}}{\if{html}{\out{<integer>}} Optional. Default is 0 minutes. Minimum transfer time for each transfer in minutes.}
    \item{\code{additionalTransferTime}}{\if{html}{\out{<integer>}} Optional. Default is 0 minutes. Additional transfer time reserved for each transfer in minutes.}
    \item{\code{transferTimeFactor}}{\if{html}{\out{<number>}} Optional. Default is 1.0 Factor to multiply minimum required transfer times with. Values smaller than 1.0 are not supported.}
    \item{\code{useRoutedTransfers}}{\if{html}{\out{<boolean>}} Optional. Default is \code{false}. Whether to use transfers routed on OpenStreetMap data.}
    \item{\code{pedestrianProfile}}{\if{html}{\out{<string>}} Different accessibility profiles for pedestrians.
Allowed values: FOOT, WHEELCHAIR.}
    \item{\code{pedestrianSpeed}}{\if{html}{\out{<number>}} Average speed for pedestrian routing in meters per second}
    \item{\code{cyclingSpeed}}{\if{html}{\out{<number>}} Average speed for bike routing in meters per second}
    \item{\code{elevationCosts}}{\if{html}{\out{<string>}} Different elevation cost profiles for street routing. Using a elevation cost profile will prefer routes with a smaller incline and smaller difference in elevation, even if the routed way is longer. - \code{NONE}: Ignore elevation data for routing. This is the default behavior - \code{LOW}: Add a low penalty for inclines. This will favor longer paths, if the elevation increase and incline are smaller. - \code{HIGH}: Add a high penalty for inclines. This will favor even longer paths, if the elevation increase and incline are smaller.
Allowed values: NONE, LOW, HIGH.}
    \item{\code{transitModes}}{\if{html}{\out{<array>}} Optional. Default is \code{TRANSIT} which allows all transit modes (no restriction). Allowed modes for the transit part. If empty, no transit connections will be computed. For example, this can be used to allow only \verb{SUBURBAN,SUBWAY,TRAM}.
Allowed values: WALK, BIKE, RENTAL, CAR, CAR_PARKING, CAR_DROPOFF, ODM, RIDE_SHARING, FLEX, DEBUG_BUS_ROUTE, DEBUG_RAILWAY_ROUTE, DEBUG_FERRY_ROUTE, TRANSIT, TRAM, SUBWAY, FERRY, AIRPLANE, BUS, COACH, RAIL, HIGHSPEED_RAIL, LONG_DISTANCE, NIGHT_RAIL, REGIONAL_FAST_RAIL, REGIONAL_RAIL, SUBURBAN, FUNICULAR, AERIAL_LIFT, OTHER, AREAL_LIFT, METRO, CABLE_CAR.}
    \item{\code{preTransitModes}}{\if{html}{\out{<array>}} Optional. Default is \code{WALK}. Does not apply to direct connections (see \code{directMode}). A list of modes that are allowed to be used for the first mile, i.e. from the coordinates to the first transit stop. Example: \verb{WALK,BIKE_SHARING}.
Allowed values: WALK, BIKE, RENTAL, CAR, CAR_PARKING, CAR_DROPOFF, ODM, RIDE_SHARING, FLEX, DEBUG_BUS_ROUTE, DEBUG_RAILWAY_ROUTE, DEBUG_FERRY_ROUTE, TRANSIT, TRAM, SUBWAY, FERRY, AIRPLANE, BUS, COACH, RAIL, HIGHSPEED_RAIL, LONG_DISTANCE, NIGHT_RAIL, REGIONAL_FAST_RAIL, REGIONAL_RAIL, SUBURBAN, FUNICULAR, AERIAL_LIFT, OTHER, AREAL_LIFT, METRO, CABLE_CAR.}
    \item{\code{postTransitModes}}{\if{html}{\out{<array>}} Optional. Default is \code{WALK}. Does not apply to direct connections (see \code{directMode}). A list of modes that are allowed to be used for the last mile, i.e. from the last transit stop to the target coordinates. Example: \verb{WALK,BIKE_SHARING}.
Allowed values: WALK, BIKE, RENTAL, CAR, CAR_PARKING, CAR_DROPOFF, ODM, RIDE_SHARING, FLEX, DEBUG_BUS_ROUTE, DEBUG_RAILWAY_ROUTE, DEBUG_FERRY_ROUTE, TRANSIT, TRAM, SUBWAY, FERRY, AIRPLANE, BUS, COACH, RAIL, HIGHSPEED_RAIL, LONG_DISTANCE, NIGHT_RAIL, REGIONAL_FAST_RAIL, REGIONAL_RAIL, SUBURBAN, FUNICULAR, AERIAL_LIFT, OTHER, AREAL_LIFT, METRO, CABLE_CAR.}
    \item{\code{directMode}}{\if{html}{\out{<string>}} # Street modes - \code{WALK} - \code{BIKE} - \code{RENTAL} Experimental. Expect unannounced breaking changes (without version bumps) for all parameters and returned structs. - \code{CAR} - \code{CAR_PARKING} Experimental. Expect unannounced breaking changes (without version bumps) for all parameters and returned structs. - \code{CAR_DROPOFF} Experimental. Expect unannounced breaking changes (without version bumps) for all perameters and returned structs. - \code{ODM} on-demand taxis from the Prima+ÖV Project - \code{RIDE_SHARING} ride sharing from the Prima+ÖV Project - \code{FLEX} flexible transports # Transit modes - \code{TRANSIT}: translates to \verb{TRAM,FERRY,AIRPLANE,BUS,COACH,RAIL,ODM,FUNICULAR,AERIAL_LIFT,OTHER} - \code{TRAM}: trams - \code{SUBWAY}: subway trains (Paris Metro, London Underground, but also NYC Subway, Hamburger Hochbahn, and other non-underground services) - \code{FERRY}: ferries - \code{AIRPLANE}: airline flights - \code{BUS}: short distance buses (does not include \code{COACH}) - \code{COACH}: long distance buses (does not include \code{BUS}) - \code{RAIL}: translates to \verb{HIGHSPEED_RAIL,LONG_DISTANCE,NIGHT_RAIL,REGIONAL_RAIL,SUBURBAN,SUBWAY} - \code{HIGHSPEED_RAIL}: long distance high speed trains (e.g. TGV) - \code{LONG_DISTANCE}: long distance inter city trains - \code{NIGHT_RAIL}: long distance night trains - \code{REGIONAL_FAST_RAIL}: deprecated, \code{REGIONAL_RAIL} will be used - \code{REGIONAL_RAIL}: regional train - \code{SUBURBAN}: suburban trains (e.g. S-Bahn, RER, Elizabeth Line, ...) - \code{ODM}: demand responsive transport - \code{FUNICULAR}: Funicular. Any rail system designed for steep inclines. - \code{AERIAL_LIFT}: Aerial lift, suspended cable car (e.g., gondola lift, aerial tramway). Cable transport where cabins, cars, gondolas or open chairs are suspended by means of one or more cables. - \code{AREAL_LIFT}: deprecated - \code{METRO}: deprecated - \code{CABLE_CAR}: deprecated
Allowed values: WALK, BIKE, RENTAL, CAR, CAR_PARKING, CAR_DROPOFF, ODM, RIDE_SHARING, FLEX, DEBUG_BUS_ROUTE, DEBUG_RAILWAY_ROUTE, DEBUG_FERRY_ROUTE, TRANSIT, TRAM, SUBWAY, FERRY, AIRPLANE, BUS, COACH, RAIL, HIGHSPEED_RAIL, LONG_DISTANCE, NIGHT_RAIL, REGIONAL_FAST_RAIL, REGIONAL_RAIL, SUBURBAN, FUNICULAR, AERIAL_LIFT, OTHER, AREAL_LIFT, METRO, CABLE_CAR.}
    \item{\code{maxPreTransitTime}}{\if{html}{\out{<integer>}} Optional. Default is 15min which is \code{900}. Maximum time in seconds for the first street leg. Is limited by server config variable \code{street_routing_max_prepost_transit_seconds}.}
    \item{\code{maxPostTransitTime}}{\if{html}{\out{<integer>}} Optional. Default is 15min which is \code{900}. Maximum time in seconds for the last street leg. Is limited by server config variable \code{street_routing_max_prepost_transit_seconds}.}
    \item{\code{maxDirectTime}}{\if{html}{\out{<integer>}} Optional. Default is 30min which is \code{1800}. Maximum time in seconds for direct connections. If a value smaller than either \code{maxPreTransitTime} or \code{maxPostTransitTime} is used, their maximum is set instead. Is limited by server config variable \code{street_routing_max_direct_seconds}.}
    \item{\code{requireBikeTransport}}{\if{html}{\out{<boolean>}} Optional. Default is \code{false}. If set to \code{true}, all used transit trips are required to allow bike carriage.}
    \item{\code{requireCarTransport}}{\if{html}{\out{<boolean>}} Optional. Default is \code{false}. If set to \code{true}, all used transit trips are required to allow car carriage.}
    \item{\code{.body}}{A list or object to be sent as the request body. If NULL (the default), the body will be constructed from any other top-level arguments you provide.}
    \item{\code{.return_as}}{A string specifying the return format. Defaults to 'list'. Options are 'list' for a parsed R list, 'raw' for the raw httr2_response object, or 'string' for the raw JSON string.}
    \item{\code{.json_parser}}{A string specifying which parser to use when .return_as = 'list'. Defaults to 'RcppSimdJson' (faster) or 'jsonlite'. Beware that their output may differ slightly.}
    \item{\code{.headers}}{A named list of extra HTTP headers to add to the request. If no 'Accept' header is provided anywhere, 'Accept: \emph{/}' will be sent by default.}
    \item{\code{.auth}}{An authentication token or method (e.g., a bearer token string).}
    \item{\code{.throttle_rate}}{A number to pass to \code{httr2::req_throttle()} to limit the rate of requests. For example, \code{15/60} means 15 requests per minute. This overrides any default rate set at client generation time.}
    \item{\code{.build_only}}{A logical. If TRUE, the function will build and return the httr2_request object without performing it. Defaults to FALSE.}
    \item{\code{.server}}{A string to override the base URL for this specific request. If provided, it will be used instead of the default server URL.}
    \item{\code{.referer}}{A string to set the Referer HTTP header.}
    \item{\code{.req_options}}{A named list of curl options passed to \code{httr2::req_options()}. Useful for timeouts, proxies, SSL, etc.}
    \item{\code{.handle_response}}{A function taking an \code{httr2_response} and returning a value. If supplied, it overrides \code{.return_as} / \code{.json_parser} handling. See helpers like \code{oa3_content_or_stop()}.}
    \item{\code{.json_auto_unbox}}{Logical. If TRUE, JSON bodies are encoded with \code{auto_unbox = TRUE} (jsonlite). Defaults to FALSE unless overridden.}
    \item{\code{.paginate}}{A logical, character string, or function to enable pagination. If TRUE (or "link_header"), uses Link headers. Other options: "page_param", "cursor_param", or a custom function. See \code{oa3_paginate()}.}
  }}

\item{engine}{Execution engine:
\itemize{
\item \code{"api"} (default): Uses the MOTIS POST API. Supports parallel backends.
\item \code{"batch"}: Uses the MOTIS CLI batch command. \strong{Recommended for very large
datasets} (millions of routes). Requires \code{data_dir}.
}}

\item{output}{The desired output format. One of:
\itemize{
\item \code{"data.frame"} (default): A tidy data frame.
\item \code{"raw_list"}: The raw parsed JSON response (only for \code{engine='api'} and non-parallel execution).
}}

\item{parallel}{Logical. Enable parallel processing for the API engine?
Defaults to \code{TRUE}.}

\item{backend}{Parallel backend for API engine: \code{"auto"}, \code{"httr2"}, or \code{"mirai"}.}

\item{batch_size}{Number of origins to process per batch/request.}

\item{max_destinations_per_batch}{Optional limit to split destinations into
multiple requests to avoid memory or timeout issues.}

\item{output_path}{Optional path to write results incrementally (\code{.csv},
\code{.duckdb}, or \strong{Directory} of \code{.parquet} files).}

\item{checkpoint_file}{Optional path for checkpointing progress (API engine only).}

\item{progress}{Logical. Display progress bar/messages?}

\item{data_dir}{Path to MOTIS data directory. Required if \code{engine='batch'}.}

\item{temp_dir}{Directory for temporary batch files. Defaults to \code{tempdir()}.}

\item{keep_files}{Logical. Keep temporary files? Defaults to \code{FALSE}.}

\item{eol}{Optional line ending for batch query files (e.g., \code{"\\n"} for LF or
\code{"\\r\\n"} for CRLF).}

\item{motis_path}{Path to the directory containing the MOTIS binary, or
\code{NULL} to use the system PATH.}
}
\value{
Depending on the \code{output} parameter and \code{output_path}, a \code{data.frame},
a list, or the \code{output_path} invisibly.
}
\description{
This function computes travel time and distance from origin(s) to
multiple destinations (or vice versa) using public transit and other
intermodal modes (walking, cycling, etc.). It supports both the MOTIS
POST API and the MOTIS CLI batch engine.
}
